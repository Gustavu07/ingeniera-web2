<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalles de Hamburguesa</title>
    <!-- Agrega Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Tu archivo CSS personalizado (opcional) -->
    <style>
        .burger-detail {
            margin-top: 30px;
        }
        .burger-detail img {
            border-radius: 10px;
        }
    </style>
</head>
<body>
    <%- include('../../components/header.ejs') %>

    <div class="container mt-5 burger-detail">
        <h1 class="text-center mb-4"><%= burger.nombre %></h1>
        
        <div class="row">
            <div class="col-md-6 mb-4">
                <img src="/images/burgers/<%= burger.id %>.jpg" alt="Foto de burger" class="img-fluid shadow-sm">
            </div>
            <div class="col-md-6">
                <h3 class="mb-3">Descripción:</h3>
                <p class="lead"><%= burger.descripcion %></p>
                <h4 class="mb-3">Precio: $<%= burger.precio %></h4>
                <h5 class="mb-3">Local:</h5>
                <p class="text-muted"><%= burger.local ? burger.local.nombre : 'No disponible' %></p> <!-- Maneja casos donde 'local' pueda ser null -->
                <a href="/reviews/<%= burger.id %>/create" class="btn btn-success btn-lg mt-3">Agregar Reseña</a>

            </div>
        </div>
    </div>

    <%- include('../../components/footer.ejs') %>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
